<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.exampleapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.cart.CartActivity"></activity>
        <activity android:name=".ui.directory.ItemViewActivity" />
        <activity android:name=".ui.directory.MobileActivity" />
        <activity android:name=".ui.directory.TabletActivity" />
        <activity android:name=".ui.directory.PCGamingActivity" />
        <activity android:name=".ui.directory.laptop.LaptopActivity" />
        <activity android:name=".ui.myaccout.MyAccoutActivity" />
        <activity android:name=".ui.electronicedevice.ElectroniceDeviceActivity" />
        <activity android:name=".ui.profile.ProfileActivity" />
        <activity android:name=".ui.onlineshop.CollectionItemsActivity" />
        <activity android:name=".ui.lockerdropoff.LockerDropOffActivity" />
        <activity android:name=".ui.register.RegisterActivity" />
        <activity android:name=".ui.login.LoginActivity" />
        <activity
            android:name=".SplashScreenActivity"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.navigation.NavigationActivity"
            android:label="@string/title_activity_navigation"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".base.BaseActivity" />
    </application>

</manifest>